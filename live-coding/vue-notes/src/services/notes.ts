const getAllNotes = () => {};

const getNote = (noteId: string) => {};

const createNote = () => {};

const updateNote = () => {};

const deleteNote = () => {};

export default { getAllNotes, getNote, createNote, updateNote, deleteNote };
