{
  "name": "notes-full-stack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed:test": "cd backend && npm run seed:test",
    "start:test": "npm run seed:test && concurrently \"cd backend && npm run start:test\" \"cd frontend && npm run dev\"",
    "test:e2e": "playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.1",
    "concurrently": "^9.2.1"
  }
}
